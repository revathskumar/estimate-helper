image: node

before_script:
  - npm install -g create-elm-app
  - npm install

cache:
  paths:
    - node_modules/

pages:
  script:
    - elm-app build
  artifacts:
    paths:
      - build
  only:
    - master
